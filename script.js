document.addEventListener("DOMContentLoaded",()=>{document.querySelectorAll(".edu-slider").forEach((slider,offset)=>{const slides=[...slider.querySelectorAll(".edu-slide")],box=slider.querySelector(".edu-dots");if(slides.length<=1||!box)return;let current=0,timer;slides.forEach((_,i)=>{const b=document.createElement("button");b.type="button";b.className="edu-dot"+(i===0?" active":"");b.setAttribute("aria-label","Show image "+(i+1));b.addEventListener("click",()=>{show(i);start()});box.appendChild(b)});const dots=[...box.querySelectorAll(".edu-dot")];function show(i){slides[current].classList.remove("active");dots[current].classList.remove("active");current=i;slides[current].classList.add("active");dots[current].classList.add("active")}function start(){clearInterval(timer);timer=setInterval(()=>show((current+1)%slides.length),6500)}slider.addEventListener("mouseenter",()=>clearInterval(timer));slider.addEventListener("mouseleave",start);setTimeout(start,offset*900)})});